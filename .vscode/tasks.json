{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "NodeTS:Build",
            "type": "shell",
            "command": "tsc --sourceMap --removeComments false --pretty --project node-typescript/tsconfig.json",
            "group": "build"
        }
    ]
}
